# clientes/

Uma pasta por cliente que a Special Vision atende com envio de e-mail.
Para criar um cliente novo: copie `_modelo-cliente/` para `clientes/<slug>/`, renomeie e preencha o `instrucoes.md`.
`slug` = minúsculas, sem acento, com hífen (ex.: `unique-cafes`).
